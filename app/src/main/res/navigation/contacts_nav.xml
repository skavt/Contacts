<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav.xml"
    app:startDestination="@id/fragment_contacts">

    <fragment
        android:id="@+id/fragment_contacts"
        android:name="com.cst.contacts.contacts.ContactsFragment"
        tools:layout="@layout/fragment_contacts">
        <action
            android:id="@+id/contact_detailed_action"
            app:destination="@id/contact_detailed" />
    </fragment>
    <fragment
        tools:layout="@layout/contact_detailed"
        android:id="@+id/contact_detailed"
        android:name="com.cst.contacts.details.ContactDetailedFragment" />
</navigation>